services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - db-data:/app/data
      - uploads:/app/public/uploads
    env_file: .env
    restart: unless-stopped

volumes:
  db-data:
  uploads:
